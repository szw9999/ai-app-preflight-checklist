{
  "name": "ai-app-preflight-checklist",
  "version": "0.1.0",
  "private": true,
  "description": "Free interactive pre-launch evidence checklist for AI-built React and TypeScript applications.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
